/**
 * ============================================================================
 * ZyneDigix GSAP Plugin Registration
 * ============================================================================
 */

import gsap from "gsap";

export function registerGSAPPlugins() {
  // Register GSAP plugins here as the project grows.
  //
  // Example:
  // gsap.registerPlugin(ScrollTrigger, SplitText, CustomEase);

  return gsap;
}