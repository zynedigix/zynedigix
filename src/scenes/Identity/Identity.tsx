const Identity = () => {
    return (
        <section
            id="identity"
            style={{
                minHeight: "100vh",
            }}
        >
            <h1>Identity</h1>
        </section>
    );
};

export default Identity;